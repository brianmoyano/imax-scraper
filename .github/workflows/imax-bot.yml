name: IMAX Showcase Bot

on:
  schedule:
    - cron: "*/30 * * * *"   # Cada 30 minutos, todos los días
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check-imax:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Check IMAX lineup and showtimes
        run: node check-imax.mjs
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Commit snapshot changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add imax_estado.json
          git diff --quiet && git diff --staged --quiet || git commit -m "Update IMAX snapshot [skip ci]"

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
